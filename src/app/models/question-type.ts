export enum QuestionType {
  DATE = 'date',
  DATE_TIME = 'dateTime',
  DECIMAL = 'decimal',
  CHOICE = 'choice',
  INTEGER = 'integer',
  QUANTITY = "quantity",
  STRING = 'string',
  TIME = 'time',
  TEXT = 'text'
}
